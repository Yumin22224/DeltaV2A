# Backend API URL.
# Leave empty for local dev (Vite proxies /api/* -> localhost:8000).
# Set to your backend URL when deploying frontend to Vercel:
#   VITE_API_URL=https://your-backend.example.com
VITE_API_URL=
